# Cloudflare Workers — Static Assets (même mécanisme que pinkin).
# Site Astro statique : assets servis depuis ./dist, sans script Worker.
name = "projetplanb"
compatibility_date = "2024-09-01"

[assets]
directory = "./dist"
