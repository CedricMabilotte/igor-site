[build]
  command = "npm run build"
  publish = "dist/"

[build.environment]
  NODE_VERSION = "22"

# Edge function lang-redirect héritée de goorg-site désactivée en phase 1.
# Réactivable quand le bilingue FR/EN sera mis en place sur certains cas d'étude.
# [[edge_functions]]
#   path = "/"
#   function = "lang-redirect"
